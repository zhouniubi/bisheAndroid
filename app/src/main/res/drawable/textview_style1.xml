<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:left="-1dp"
        android:right="-1dp"
        android:top="-1dp">
        <shape>
            <!-- 背景颜色 -->
            <solid android:color="@color/smssdk_gray_press" />

            <!-- 边框的宽度和颜色颜色 -->
            <stroke

                android:width="1dip"
                android:color="#DBD9D9" />

            <!-- android:radius 弧形的半径 -->
            <corners android:radius="20dp" />

            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding
                android:bottom="10dp"
                android:left="10dp"
                android:right="10dp"
                android:top="10dp" />
        </shape>
    </item>
</layer-list>

